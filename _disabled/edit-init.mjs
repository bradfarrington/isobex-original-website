// Neutralized stub for the original editor bundle. The site is read-only static hosting —
// there is no in-browser editor. This no-op keeps the dynamic import resolvable (no 404)
// while doing nothing.
export const createEditorBar = () => {};
export default {};
